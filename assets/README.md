Lege hier deine 3D-Modelle ab:

- `skeleton.glb` (Pflicht fuer produktives Modell)
- `muscles.glb` (optional fuer Layer-System)

Wenn `skeleton.glb` fehlt, nutzt die App automatisch ein eingebautes Platzhalter-Modell.
